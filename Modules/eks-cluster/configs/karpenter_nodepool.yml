apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: ${nodepool_name}
spec:
  # Template section that describes how to template out NodeClaim resources that Karpenter will provision
  # Karpenter will consider this template to be the minimum requirements needed to provision a Node using this NodePool
  # It will overlay this NodePool with Pods that need to schedule to further constrain the NodeClaims
  # Karpenter will provision to launch new Nodes for the cluster
  template:
    metadata:
      # Labels are arbitrary key-values that are applied to all nodes
      labels: ${karpenter_labels}

    spec:
      # References the Cloud Provider's NodeClass resource, see your cloud provider specific documentation
      nodeClassRef:
        group: karpenter.k8s.aws  # Updated since only a single version will be served
        kind: EC2NodeClass
        name: ${nodeclass_name}

      # The amount of time a Node can live on the cluster before being removed
      # Avoiding long-running Nodes helps to reduce security vulnerabilities as well as to reduce the chance of issues that can plague Nodes with long uptimes such as file fragmentation or memory leaks from system processes
      # You can choose to disable expiration entirely by setting the string value 'Never' here

      # Note: changing this value in the nodepool will drift the nodeclaims.
      expireAfter: Never

      # The amount of time that a node can be draining before it's forcibly deleted. A node begins draining when a delete call is made against it, starting
      # its finalization flow. Pods with TerminationGracePeriodSeconds will be deleted preemptively before this terminationGracePeriod ends to give as much time to cleanup as possible.
      # If your pod's terminationGracePeriodSeconds is larger than this terminationGracePeriod, Karpenter may forcibly delete the pod
      # before it has its full terminationGracePeriod to cleanup.

      # Note: changing this value in the nodepool will drift the nodeclaims.
      terminationGracePeriod: 48h

      # Requirements that constrain the parameters of provisioned nodes.
      # These requirements are combined with pod.spec.topologySpreadConstraints, pod.spec.affinity.nodeAffinity, pod.spec.affinity.podAffinity, and pod.spec.nodeSelector rules.
      # Operators { In, NotIn, Exists, DoesNotExist, Gt, and Lt } are supported.
      # https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#operators
      requirements:
        - key: "karpenter.k8s.aws/instance-category"
          operator: In
          values: ${karpenter_instance_category}
          # minValues here enforces the scheduler to consider at least that number of unique instance-category to schedule the pods.
          # This field is ALPHA and can be dropped or replaced at any time
        - key: "karpenter.k8s.aws/instance-family"
          operator: In
          values: ${karpenter_instance_family}
        - key: node.kubernetes.io/instance-type
          operator: In
          values: ${karpenter_instance_type}
        - key: "karpenter.k8s.aws/instance-hypervisor"
          operator: In
          values: ["nitro"]
        - key: "karpenter.k8s.aws/instance-generation"
          operator: Gt
          values: ["2"]
        - key: "topology.kubernetes.io/zone"
          operator: In
          values: ${karpenter_instance_azs}
        - key: "kubernetes.io/arch"
          operator: In
          values: ${karpenter_instance_architecture}
        - key: "karpenter.sh/capacity-type"
          operator: In
          values: ${karpenter_capacity_type}

  # Disruption section which describes the ways in which Karpenter can disrupt and replace Nodes
  # Configuration in this section constrains how aggressive Karpenter can be with performing operations
  # like rolling Nodes due to them hitting their maximum lifetime (expiry) or scaling down nodes to reduce cluster cost
  disruption:
    # Describes which types of Nodes Karpenter should consider for consolidation
    # If using 'WhenEmptyOrUnderutilized', Karpenter will consider all nodes for consolidation and attempt to remove or replace Nodes when it discovers that the Node is empty or underutilized and could be changed to reduce cost
    # If using `WhenEmpty`, Karpenter will only consider nodes for consolidation that contain no workload pods
    consolidationPolicy: ${karpenter_consolidation_policy}

    # The amount of time Karpenter should wait to consolidate a node after a pod has been added or removed from the node.
    # You can choose to disable consolidation entirely by setting the string value 'Never' here
    consolidateAfter: ${karpenter_consolidate_after}

    # Budgets control the speed Karpenter can scale down nodes.
    # Karpenter will respect the minimum of the currently active budgets, and will round up
    # when considering percentages. Duration and Schedule must be set together.
    budgets:
    - nodes: ${karpenter_max_nodes_disruption_allowed_when_underutilized_in_schedule}
      schedule: ${karpenter_disruption_allowed_when_underutilized_schedule_start}
      duration: ${karpenter_disruption_allowed_when_underutilized_schedule_duration}
      reasons: 
      - "Drifted"
      - "Underutilized"
    - nodes: ${karpenter_nodes_disruption_allowed_when_empty_all_the_times}
      reasons:
      - Empty
    - nodes: ${karpenter_nodes_disruption_allowed_at_any_time_of_the_day}
      reasons:
      - Drifted
      - Underutilized
    
  # Resource limits constrain the total size of the pool.
  # Limits prevent Karpenter from creating new instances once the limit is exceeded.
  limits:
    cpu: ${karpenter_cpu_limit}
    memory: ${karpenter_memory_limit}

  # Priority given to the NodePool when the scheduler considers which NodePool
  # to select. Higher weights indicate higher priority when comparing NodePools.
  # Specifying no weight is equivalent to specifying a weight of 0.
  weight: 10


###### Old ##############


# apiVersion: karpenter.sh/v1
# kind: NodePool
# metadata:
#   name: default
# spec:
#   # Template section that describes how to template out NodeClaim resources that Karpenter will provision
#   # Karpenter will consider this template to be the minimum requirements needed to provision a Node using this NodePool
#   # It will overlay this NodePool with Pods that need to schedule to further constrain the NodeClaims
#   # Karpenter will provision to launch new Nodes for the cluster
#   template:
#     metadata:
#       # Labels are arbitrary key-values that are applied to all nodes
#       labels: ${karpenter_labels}

#     spec:
#       # References the Cloud Provider's NodeClass resource, see your cloud provider specific documentation
#       nodeClassRef:
#         group: karpenter.k8s.aws  # Updated since only a single version will be served
#         kind: EC2NodeClass
#         name: default

#       # The amount of time a Node can live on the cluster before being removed
#       # Avoiding long-running Nodes helps to reduce security vulnerabilities as well as to reduce the chance of issues that can plague Nodes with long uptimes such as file fragmentation or memory leaks from system processes
#       # You can choose to disable expiration entirely by setting the string value 'Never' here

#       # Note: changing this value in the nodepool will drift the nodeclaims.
#       expireAfter:  Never

#       # The amount of time that a node can be draining before it's forcibly deleted. A node begins draining when a delete call is made against it, starting
#       # its finalization flow. Pods with TerminationGracePeriodSeconds will be deleted preemptively before this terminationGracePeriod ends to give as much time to cleanup as possible.
#       # If your pod's terminationGracePeriodSeconds is larger than this terminationGracePeriod, Karpenter may forcibly delete the pod
#       # before it has its full terminationGracePeriod to cleanup.

#       # Note: changing this value in the nodepool will drift the nodeclaims.
#       terminationGracePeriod: 48h

#       # Requirements that constrain the parameters of provisioned nodes.
#       # These requirements are combined with pod.spec.topologySpreadConstraints, pod.spec.affinity.nodeAffinity, pod.spec.affinity.podAffinity, and pod.spec.nodeSelector rules.
#       # Operators { In, NotIn, Exists, DoesNotExist, Gt, and Lt } are supported.
#       # https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#operators
#       requirements:
#         - key: "karpenter.k8s.aws/instance-category"
#           operator: In
#           values: ${karpenter_instance_category}   # ["c", "m", "r"]
#           # minValues here enforces the scheduler to consider at least that number of unique instance-category to schedule the pods.
#           # This field is ALPHA and can be dropped or replaced at any time
#         - key: "karpenter.k8s.aws/instance-family"
#           operator: In
#           values: ${karpenter_instance_family}  # ["m5","m5d","c5","c5d","c4","r4"]
#         - key: node.kubernetes.io/instance-type
#           operator: In
#           values: ${karpenter_instance_type}           # ["c5a.xlarge", "m5a.16xlarge"]
#         - key: "karpenter.k8s.aws/instance-hypervisor"
#           operator: In
#           values: ["nitro"]
#         - key: "karpenter.k8s.aws/instance-generation"
#           operator: Gt
#           values: ["2"]
#         - key: "topology.kubernetes.io/zone"
#           operator: In
#           values: ${karpenter_instance_azs}  # ["us-west-2a", "us-west-2b"]
#         - key: "kubernetes.io/arch"
#           operator: In
#           values: ${karpenter_instance_architecture} # ["arm64", "amd64"]
#         - key: "karpenter.sh/capacity-type"
#           operator: In
#           values:  ${karpenter_capacity_type} # ["spot", "on-demand"]

#   # Disruption section which describes the ways in which Karpenter can disrupt and replace Nodes
#   # Configuration in this section constrains how aggressive Karpenter can be with performing operations
#   # like rolling Nodes due to them hitting their maximum lifetime (expiry) or scaling down nodes to reduce cluster cost
#   disruption:
#     # Describes which types of Nodes Karpenter should consider for consolidation
#     # If using 'WhenEmptyOrUnderutilized', Karpenter will consider all nodes for consolidation and attempt to remove or replace Nodes when it discovers that the Node is empty or underutilized and could be changed to reduce cost
#     # If using `WhenEmpty`, Karpenter will only consider nodes for consolidation that contain no workload pods
#     consolidationPolicy: ${karpenter_consolidation_policy}    # WhenEmptyOrUnderutilized | WhenEmpty

#     # The amount of time Karpenter should wait to consolidate a node after a pod has been added or removed from the node.
#     # You can choose to disable consolidation entirely by setting the string value 'Never' here
#     consolidateAfter: ${karpenter_consolidate_after}   # 1m | Never # Added to allow additional control over consolidation aggressiveness

#     # Budgets control the speed Karpenter can scale down nodes.
#     # Karpenter will respect the minimum of the currently active budgets, and will round up
#     # when considering percentages. Duration and Schedule must be set together.
#     budgets:
#     - nodes: ${karpenter_max_nodes_disruption_allowed_when_underutilized_in_schedule} # "0"
#       schedule: ${karpenter_disruption_allowed_when_underutilized_schedule_start} # "3 15 * * mon-fri"
#       duration: ${karpenter_disruption_allowed_when_underutilized_schedule_duration} # 8h
#       reasons: 
#       - "Drifted"
#       - "Underutilized"
#     - nodes: ${karpenter_nodes_disruption_allowed_when_empty_all_the_times} # "100%"
#       reasons:
#       - Empty
#     - nodes: ${karpenter_nodes_disruption_allowed_at_any_time_of_the_day} # "10%"
#       reasons:
#       - Drifted
#       - Underutilized
    

#   # Resource limits constrain the total size of the pool.
#   # Limits prevent Karpenter from creating new instances once the limit is exceeded.
#   limits:
#     cpu: ${karpenter_cpu_limit}
#     memory: ${karpenter_memory_limit}

#   # Priority given to the NodePool when the scheduler considers which NodePool
#   # to select. Higher weights indicate higher priority when comparing NodePools.
#   # Specifying no weight is equivalent to specifying a weight of 0.
#   weight: 10

